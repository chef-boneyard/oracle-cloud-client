require 'spec_helper'

describe Oracle::Cloud::Client do
  it 'has a version number' do
    expect(Oracle::Cloud::Client::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
